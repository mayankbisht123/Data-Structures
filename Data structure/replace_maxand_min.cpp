#include<stdio.h>
int main()
{
    int n,max,min,p,q;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    {

        scanf("%d",&a[i]);
    }
    max=min=a[0];
    for(i=0;i<n;i++)
    {
        if(max<a[i])
    }
}
